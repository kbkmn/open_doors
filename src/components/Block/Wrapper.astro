---
import { cn } from "@/utils/classname"

type Props = {
  id?: string
  class?: string
  gap?: number
}

const { id, class: className, gap } = Astro.props
---

<div
  id={id}
  class={cn("px-4 pb-8 md:py-16", className)}
  data-gap={gap}
>
  <slot />
</div>
