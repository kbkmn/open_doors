---
import { cn } from "@/utils/classname"

type Props = {
  class?: string
}

const { class: className } = Astro.props
---

<div
  class={cn(
    "leading-7 [&>_*]:my-4 [&_:is(ol,ul)]:pl-4 [&_ol]:list-decimal [&_ul]:list-disc [&_a]:underline [&_a]:text-blue [&_a:hover]:no-underline [&_h4]:text-[1.125rem] [&_h5]:text-[1rem] [&_h3]:text-[1.25rem] [&_h2]:text-[1.375rem] [&_h1]:text-[1.5rem] [&_:is(h1,h2,h3,h4,h5)]:leading-9 [&_:is(h1,h2,h3,h4,h5)]:font-bold after:block after:clear-both",
    className
  )}
>
  <slot />
</div>
